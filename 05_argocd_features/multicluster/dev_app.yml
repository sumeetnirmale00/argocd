apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sumeetnirmale00/argocd.git
    targetRevision: main
    path: argocd-demos/ui_approach/nginx        # Path to nginx manifests
  destination:
    server: https://kubernetes.default.svc      # from `argocd cluster list`
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true